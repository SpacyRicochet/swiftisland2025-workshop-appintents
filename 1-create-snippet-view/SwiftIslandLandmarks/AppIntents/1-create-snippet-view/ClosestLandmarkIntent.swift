import AppIntents
import SwiftData
import SwiftUI

// We're going to make an intent that would look up the closest landmark.
// To make it easy, just pick the one that contains "Prins Hendrik" instead.
struct ClosestLandmarkIntent: AppIntent {

}
