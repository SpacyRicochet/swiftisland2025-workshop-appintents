import SwiftUI
import AppIntents

struct LandmarkSnippetView: View {
	// Make sure the landmark can be passed to future intents!
	
	var body: some View {
		// We want to show the landmark's name
		// and favorite status.
		EmptyView()
	}
}
